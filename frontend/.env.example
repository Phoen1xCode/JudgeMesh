# Copy to .env.local and adjust as needed.
# Leave empty to use the Vite dev proxy (recommended for local dev).
# Set to the gateway URL when running the production build directly.
VITE_API_BASE=http://localhost:8080
