# helm install loki grafana/loki-stack -n judgemesh-observe -f .
loki:
  enabled: true
  persistence:
    enabled: true
    size: 10Gi
promtail:
  enabled: true
grafana:
  enabled: false # 用 kube-prometheus-stack 里那个
